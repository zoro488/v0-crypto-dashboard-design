rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MODO DESARROLLO: Reglas permisivas para testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
